
describe('Data Randomization', () =>{
	describe('dataRandom(num_custom_lists, num_series, num_releases0', () => {
		it('should at least run', ()=>{
			var data = dataRandom(0, 100, 100);
			buildPopup(data);
			debugger;
		});
	});
});